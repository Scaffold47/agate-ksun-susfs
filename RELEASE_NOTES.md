first public build

built and tested on my own xiaomi 11t

kernelsu next legacy is built into the kernel

manual hooks are enabled

susfs is built into the kernel

susfs initializes successfully during boot

susfs userspace communication was tested after boot

tested on crdroid 12 10 android 16

use this build at your own risk

keep a working boot image from the exact rom version you are using

other roms firmware bases and android versions are not tested
